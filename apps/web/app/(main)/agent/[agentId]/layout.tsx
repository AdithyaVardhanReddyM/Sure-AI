import { AgentLayout } from "@/modules/agent/ui/agent-layout";

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <AgentLayout>{children}</AgentLayout>;
};

export default Layout;
